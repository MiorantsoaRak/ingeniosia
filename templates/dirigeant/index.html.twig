{% extends 'base.html.twig' %}

{% block title %}Dirigeant index{% endblock %}

{% block body %}
    <h1>Dirigeant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomPrenom</th>
                <th>Sexe</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dirigeant in dirigeants %}
            <tr>
                <td>{{ dirigeant.id }}</td>
                <td>{{ dirigeant.nomPrenom }}</td>
                <td>{{ dirigeant.sexe }}</td>
                <td>{{ dirigeant.email }}</td>
                <td>
                    <a href="{{ path('dirigeant_show', {'id': dirigeant.id}) }}">show</a>
                    <a href="{{ path('dirigeant_edit', {'id': dirigeant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('dirigeant_new') }}">Create new</a>
{% endblock %}
