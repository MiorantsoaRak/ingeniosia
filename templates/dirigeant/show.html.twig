{% extends 'base.html.twig' %}

{% block title %}Dirigeant{% endblock %}

{% block body %}
    <h1>Dirigeant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dirigeant.id }}</td>
            </tr>
            <tr>
                <th>NomPrenom</th>
                <td>{{ dirigeant.nomPrenom }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ dirigeant.sexe }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ dirigeant.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dirigeant_index') }}">back to list</a>

    <a href="{{ path('dirigeant_edit', {'id': dirigeant.id}) }}">edit</a>

    {{ include('dirigeant/_delete_form.html.twig') }}
{% endblock %}
