<nav class="navbar navbar-dark bg-primary navbar-expand-lg ">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main') }}">Accueil</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('code_postal_index') }}">Code Postal</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ville_index') }}">Villes</a>
                </li>
                {# <li class="nav-item">
                        <a class="nav-link" href="{{ path('type_societe_show') }}">Type sociétés</a>
                    </li>#}
            </ul>
        </div>
    </div>
</nav>