<form method="post" action="{{ path('code_postal_delete', {'id': code_postal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_postal.id) }}">
    <button class="btn">Delete</button>
</form>
